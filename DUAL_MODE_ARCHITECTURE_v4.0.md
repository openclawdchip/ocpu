# OCPU Dual-Mode Architecture v4.0
# 双模处理器架构设计文档

**版本**: 4.0  
**日期**: 2026-02-13  
**架构**: Human Mode (OOO) + Agent Mode (Static Scheduled)

---

## 1. 架构概述

OCPU v4.0引入革命性的**双模处理器架构**，支持两种截然不同的执行模式：

### 1.1 人类模式 (Human Mode)

**设计理念**: 传统OOO乱序执行，最大化单线程性能

**核心特性**:
- 16发射超标量流水线
- 256-entry ROB乱序执行
- 动态分支预测 (BTB/GHB/RAS/TAGE)
- Cache层次结构 (L1-I/L1-D/L2)
- 512物理寄存器重命名
- 支持RISC-V RV64I/M/F/D/V

**适用场景**:
- 通用计算
- 人类编写的软件执行
- 需要高单线程性能的任务

### 1.2 Agent模式 (Agent Mode)

**设计理念**: 确定性静态调度，最大化并行效率和可预测性

**核心特性**:
- 静态超标量调度 (无OOO)
- 软件流水线 (Software Pipelining)
- SRAM直接访问 (无Cache)
- Agent实时指令生成
- 确定性执行时间
- 16 Bank并行SRAM访问

**适用场景**:
- AI/ML推理加速
- 实时系统
- 确定性计算
- Agent自主代码生成与执行

---

## 2. 架构对比

| 特性 | Human Mode | Agent Mode |
|------|------------|------------|
| **执行模型** | 乱序执行 (OOO) | 静态调度 (In-Order) |
| **调度方式** | 动态调度 | Agent静态调度 |
| **指令来源** | 内存加载 | Agent实时生成 |
| **存储模型** | Cache层次 | 直接SRAM访问 |
| **延迟特性** | 可变 (Cache未命中) | 确定性 (SRAM) |
| **分支预测** | 动态预测 | 软件流水/无分支 |
| **寄存器** | 512物理寄存器 | 专用Agent寄存器 |
| **并行性** | ILP提取 | 显式并行调度 |
| **代码概念** | 软件/二进制 | 无软件，Agent生成 |
| **优化目标** | 单线程性能 | 吞吐量/确定性 |

---

## 3. 人类模式详解

### 3.1 微架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Human Mode Architecture                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │   IFetch     │───▶│   Decode     │───▶│   Rename     │      │
│  │   256KB L1-I │    │   16-wide    │    │   512 PREG   │      │
│  │   OOO Fetch  │    │   RVC        │    │   RAT        │      │
│  └──────────────┘    └──────────────┘    └──────────────┘      │
│         │                                            │          │
│         ▼                                            ▼          │
│  ┌──────────────┐                          ┌──────────────┐    │
│  │   Branch     │                          │   Issue      │    │
│  │   Predictor  │                          │   OOO Sched  │    │
│  │   BTB/GHB    │                          │   10SX/6MX   │    │
│  └──────────────┘                          └──────────────┘    │
│                                                     │           │
│         ┌───────────────────────────────────────────┘           │
│         ▼                                                       │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐     │
│  │   Execute    │───▶│   LoadStore  │───▶│   Commit     │     │
│  │   44 EU      │    │   64KB L1-D  │    │   256 ROB    │     │
│  │   6FPU/8VPU  │    │   OOO Mem    │    │   Precise    │     │
│  └──────────────┘    └──────────────┘    └──────────────┘     │
│         │                  │                                    │
│         ▼                  ▼                                    │
│  ┌──────────────────────────────────────┐                      │
│  │   L2 Cache 4MB, 16-way, MESI         │                      │
│  └──────────────────────────────────────┘                      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 关键模块

| 模块 | 配置 | 说明 |
|------|------|------|
| **IFetch** | 256KB L1-I, 8-way | BTB 2048, GHB 8192, RAS 32 |
| **Decode** | 16-wide | RVC支持, 异常检测 |
| **Rename** | 512 PREG | 16检查点, 256 RCQ |
| **Issue** | OOO | 10 SX + 6 MX + 8 LSQ |
| **Execute** | 44 EU | 10 SX + 6 MX + 6 FPU + 8 VPU + 8 LSU |
| **Commit** | 256 ROB | 16-wide提交, 精确异常 |
| **L2** | 4MB, 16-way | MESI一致性 |

---

## 4. Agent模式详解

### 4.1 微架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Agent Mode Architecture                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Agent Code Generator                          │   │
│  │  - 实时机器码生成                                          │   │
│  │  - 任务描述符 -> 机器码                                    │   │
│  │  - 无软件/二进制概念                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│                              ▼                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Static Scheduler                              │   │
│  │  - 确定性调度表                                            │   │
│  │  - 软件流水线引擎                                          │   │
│  │  - 16-wide静态发射                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│                              ▼                                   │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │   Execute    │───▶│   SRAM Ctrl  │───▶│   Execute    │      │
│  │   16 ALU     │    │   16 Bank    │    │   Complete   │      │
│  │   Static     │    │   Direct     │    │   Determin.  │      │
│  └──────────────┘    └──────────────┘    └──────────────┘      │
│         │                  │                                     │
│         ▼                  ▼                                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │   SRAM Array (512KB, 16 Bank, No Cache)                  │   │
│  │   - Bank 0-15: 32KB each                                 │   │
│  │   - 2-cycle deterministic latency                        │   │
│  │   - 16 concurrent ports                                  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │   Software Pipelining Engine                              │   │
│  │   - Prolog / Kernel / Epilog generation                   │   │
│  │   - Modulo scheduling                                     │   │
│  │   - Kernel unrolling                                      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 核心创新

#### 4.2.1 Agent代码生成器

**概念**: 无需传统软件，Agent根据任务描述符实时生成最优机器码

```
任务描述符 (128-bit):
┌────────┬────────┬────────┬────────┬────────┬────────┬────────┐
│TaskType│DataSize│Iterations│Parallel│MemPattrn│CompIntens│DataAddr│
│ 8-bit  │ 16-bit │ 16-bit  │ 8-bit  │ 8-bit   │ 16-bit   │ 32-bit │
└────────┴────────┴────────┴────────┴────────┴────────┴────────┘

↓ Agent Code Generator

机器码序列 (512-bit x N):
┌────────────────────────────────────────────────────────────────┐
│ 16 instructions (32-bit each) packed in 512-bit stream          │
│ - Static scheduled                                               │
│ - Deterministic latency                                          │
│ - SRAM direct addresses                                          │
└────────────────────────────────────────────────────────────────┘
```

**代码生成流程**:
1. 解析任务描述符
2. 选择代码模板
3. 定制化代码生成
4. 静态调度优化
5. 软件流水线化
6. 输出生成代码

#### 4.2.2 静态调度单元

**确定性调度**: 每周期发射16条指令，无动态依赖检查

```
调度表 (256 entries):
┌──────────┬──────────┬──────────┬──────────┬──────────┐
│  Inst    │  Cycle   │  Slot    │  Src0    │  Dst     │
│  Raw     │  Offset  │  (0-15)  │  Reg     │  Reg     │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│  ADD     │    0     │    0     │   R1     │   R2     │
│  LD      │    0     │    1     │   R3     │   R4     │
│  MAC     │    1     │    2     │   R4     │   R5     │
│  ...     │   ...    │   ...    │   ...    │   ...    │
└──────────┴──────────┴──────────┴──────────┴──────────┘
```

**特点**:
- 编译时/配置时确定调度
- 无记分板，无动态唤醒
- 确定性执行时间
- 适合实时系统

#### 4.2.3 SRAM直接访问

**无Cache设计**: 直接访问16 Bank SRAM，确定性延迟

```
SRAM架构:
┌─────────────────────────────────────────────────────────────┐
│                    512KB SRAM Array                          │
├─────────────────────────────────────────────────────────────┤
│  Bank 0  │ Bank 1  │ Bank 2  │ ... │ Bank 14 │ Bank 15     │
│  32KB    │  32KB   │  32KB   │     │  32KB   │  32KB       │
│  ┌────┐  │  ┌────┐ │  ┌────┐ │     │  ┌────┐ │  ┌────┐     │
│  │    │  │  │    │ │  │    │ │     │  │    │ │  │    │     │
│  └────┘  │  └────┘ │  └────┘ │     │  └────┘ │  └────┘     │
│    ↑     │    ↑    │    ↑    │     │    ↑    │    ↑        │
│  Port 0  │ Port 1  │ Port 2  │     │ Port 14 │ Port 15     │
└─────────────────────────────────────────────────────────────┘

特性:
- 16并发访问端口
- 2周期确定性延迟
- 无缓存未命中惩罚
- 显式Bank分配
```

#### 4.2.4 软件流水线引擎

**自动软件流水化**: Agent自动将循环转换为流水线形式

```
原始循环:
for (i = 0; i < N; i++) {
    load A[i];
    load B[i];
    C[i] = A[i] + B[i];
    store C[i];
}

↓ Software Pipelining Engine

流水线形式 (II=1):
Cycle 0 (Prolog):  LD A[0]
Cycle 1 (Prolog):  LD A[1]  |  LD B[0]
Cycle 2 (Kernel):  LD A[2]  |  LD B[1]  |  ADD C[0]  |  ST C[0]
Cycle 3 (Kernel):  LD A[3]  |  LD B[2]  |  ADD C[1]  |  ST C[1]
...
Cycle N (Epilog):                           ADD C[N-1] |  ST C[N-1]
```

---

## 5. 模式切换

### 5.1 切换流程

```
Human Mode Active
       │
       │ Mode Switch Request
       ▼
┌──────────────┐
│ Save Context │  <-- 保存人类模式上下文
│ (PC, Regs)   │
└──────────────┘
       │
       ▼
┌──────────────┐
│ Flush Cache  │  <-- 刷新Cache到SRAM
└──────────────┘
       │
       ▼
┌──────────────┐
│ Restore Agent│  <-- 恢复Agent模式上下文
│ Context      │
└──────────────┘
       │
       ▼
Agent Mode Active
```

### 5.2 上下文定义

| 上下文 | Human Mode | Agent Mode |
|--------|------------|------------|
| **PC** | 64-bit物理地址 | 任务描述符指针 |
| **Regs** | 512物理寄存器 | 专用Agent寄存器 |
| **State** | ROB/队列状态 | 调度表位置 |
| **Cache** | L1/L2 Cache内容 | SRAM Bank状态 |

---

## 6. 编程模型

### 6.1 人类模式

**传统编程**:
- C/C++编译为RISC-V二进制
- 标准操作系统支持
- 动态链接库

### 6.2 Agent模式

**任务描述符编程**:

```c
// 无需传统代码，直接提交任务描述符
task_descriptor_t task = {
    .task_type = TASK_VEC_ADD,
    .data_size = 4096,
    .iterations = 1,
    .parallelism = 16,
    .data_address = 0x80000000
};

agent_submit_task(task);  // Agent生成并执行最优代码
```

**特点**:
- 无编译过程
- 无二进制文件
- 运行时自适应代码生成
- 自动并行化与优化

---

## 7. 性能对比

| 指标 | Human Mode | Agent Mode |
|------|------------|------------|
| **单线程IPC** | 12-14 | 16 (确定性) |
| **向量吞吐量** | 3 TFLOPS | 4 TFLOPS |
| **内存延迟** | 2-50ns (Cache) | 2ns (SRAM) |
| **延迟确定性** | 变化大 | 100%确定 |
| **代码大小** | MB级 | 无 (实时生成) |
| **启动时间** | ms级 | us级 |
| **功耗效率** | 高 | 极高 |

---

## 8. 应用场景

### 8.1 人类模式适用

- 通用操作系统运行
- 人类编写的应用程序
- 需要复杂控制流的任务
- 兼容性要求高的场景

### 8.2 Agent模式适用

- AI/ML推理加速
- 实时控制 (RT核)
- 流处理 (视频/音频)
- 确定性计算 (金融/通信)
- Agent自主系统

---

## 9. RTL实现

### 9.1 新增模块

```
rtl/
├── agent_mode/
│   ├── ocpu_agent_static_scheduler.sv  # 静态调度单元
│   ├── ocpu_agent_sram_controller.sv   # SRAM控制器
│   ├── ocpu_agent_code_generator.sv    # 代码生成器
│   └── ocpu_agent_sw_pipeline.sv       # 软件流水线引擎
├── core/
│   └── ocpu_dual_mode_controller.sv    # 双模式控制器
└── include/
    └── ocpu_dual_mode_defs.sv          # 双模式定义
```

### 9.2 关键参数

```systemverilog
// 模式定义
localparam MODE_HUMAN = 2'b01;
localparam MODE_AGENT = 2'b10;

// Agent模式配置
localparam NUM_SRAM_BANKS = 16;
localparam SRAM_BANK_SIZE = 32768;
localparam STATIC_ISSUE_WIDTH = 16;
localparam MAX_SW_PIPELINE_DEPTH = 8;
```

---

## 10. 总结

OCPU v4.0双模架构代表了处理器设计的范式转变：

1. **人类模式**: 保留传统OOO优势，兼容现有软件生态
2. **Agent模式**: 开创性静态调度+实时代码生成，面向未来AI原生计算

**核心价值**:
- 一套硬件，两种计算范式
- 软件定义 vs Agent定义
- 灵活性 vs 确定性
- 兼容性 vs 性能极致

**未来展望**:
- 更多Agent模式任务类型
- 自适应代码生成优化
- 多Agent协作调度
- 云端Agent代码分发
